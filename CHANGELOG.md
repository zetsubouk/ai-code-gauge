# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [Unreleased]

### Fixed
- 趋势图首日误读：改为固定 7 个槽位（最右为最新一天），无数据的天以淡色占位柱显示，单条数据不再被拉伸成酷似进度条的整行横线。

## [1.4.0] - 2026-09-08

### Added
- **用量历史与 7 日趋势**：每次刷新成功后记录每日用量快照（本地存储，按天去重、滚动保留 30 天），额度卡片下方新增近 7 日迷你趋势柱条，纯 CSS 实现、零依赖。
- **阈值提醒通知**：任一额度窗口 ≥95% 时发送系统通知；设置中可开关（默认关闭），同一窗口 6 小时冷却，避免重复打扰。
- **配置导出 / 导入**：设置页可导出/导入 JSON 配置用于备份迁移；API Key 默认不导出，勾选后才包含；导入时空 Key 视为保留现有密钥。
- **单元测试**：`tests/` 基于 Node 内置 test runner（零依赖），覆盖 shared 纯函数、接口解析与错误分类、后台 `refresh()` 集成（快照结构、徽章顺序、故障隔离、通知冷却）；`npm test` 一键运行。
- **CI**：GitHub Actions 在 push/PR 跑语法检查与单元测试，打 `v*` tag 自动打包并上传 zip artifact。
- **docs/ROADMAP.md**：记录后续排期（GLM 国际版/团队版、供应商注册表、i18n、商店上架等）。

### Changed
- **仓库整理**：合并并删除遗留的 `trae/agent-*` 残留分支（其改动均已包含于 main）；清理元数据中的旧品牌残留（package.json 描述、后台日志前缀、图标脚本与弹窗注释），README 结构树补充 `design/`，移除弹窗中后台永不返回的 `no_key` 死分支。
- **阈值与窗口识别收敛为单一来源**：`THRESHOLDS`、`classifyWindow()`、`pctState()` 落在 `shared/`，弹窗与后台共用，消除三处 80/95 阈值与两处窗口魔数的漂移风险。
- **共享网络层 `shared/net.js`**：统一超时控制，网络级瞬态失败自动重试一次（业务错误不重试）；GLM/Go 两处重复的超时样板合并。
- **徽章百分比钳制 0–100**，防接口异常值。
- **弹窗渲染加固**：额度卡片与 MCP 行改为 DOM 构建 + `textContent`，动态文本不再经过 `innerHTML`。
- **可访问性**：进度条补 `role="progressbar"` 与取值标注；错误/状态栏补 `aria-live`；图标按钮补 `aria-label` 与 `:focus-visible` 焦点环；`prefers-reduced-motion` 下关闭动画；面板标题供应商名可截断防溢出；muted 文字对比度微调。
- Go 各窗口美元限额展示口径改为「参考限额」，并在 docs/API.md 注明该数值仅对已知档位成立。

### Fixed
- 弹窗 MCP 行在账号无总额数据时会显示「NaN 次」的问题（`nf()` 对不可转数字的输入统一显示「—」）。
- `scripts/package.py` 打包清单改为从 manifest.json 推导并校验引用完整性，防止未来新增运行时文件（如 `_locales/`）被静默漏打包；同时校验 manifest 与 package.json 版本号一致。
- `scripts/test-api.mjs` 失败时退出码非 0（原先恒为 0），支持 `GO_KEY` 冒烟 OpenCode Go。
- 文档纠偏：INSTALL.md 更新新版商店域名并补 Python/Pillow 前置与调试脚本用法；SECURITY.md 补 OpenCode Go 密钥边界并改用 GitHub 私有漏洞报告渠道指引。

## [1.3.1] - 2026-09-06

### Changed
- **扩展名称纯英文化**：manifest `name` 由「AI Coding Gauge — Coding Plan 用量监控」改为「AI Coding Gauge - Coding Plan Usage Monitor」，保持商店展示名全英文；弹窗内界面文案不变（仍为中文）。功能与 1.3.0 完全一致。

## [1.3.0] - 2026-09-06

### Added
- **OpenCode Go 到期日推算**：面板标题行显示当月套餐到期日期，由「每月」额度窗口的自动重置时间（endTs）推算（本地时区取日历日）；临近到期 ≤7 天变黄、已过变红，与 GLM 到期提醒规则一致；接口未返回重置时间时不显示。

### Changed
- **品牌英文化**：显示名称「AI 码表」改为「AI Coding Gauge」（弹窗标题、设置页、manifest 同步），头部保留小号「用量监控」中文辅助标识。
- **订阅信息随面板**：GLM 套餐等级徽章与到期日期从头部移入「智谱 GLM」面板标题行；头部收敛为单行品牌区，顶部新增品牌渐变条。
- **UI 打磨**：设置/刷新按钮换为线性 SVG 图标，修复刷新动画结束后图标被覆盖的问题；百分比数字加大至 24px；面板与卡片层次微调；底部状态栏增加细线分隔；徽章/到期无数据时不再渲染空占位。

## [1.2.0] - 2026-09-06

### Added
- **品牌重塑**：项目更名为「AI 码表 CodeGauge」（原 GLM Coding Plan 用量监控），启用供应商中立的仪表盘 logo（深色渐变圆角底 + 弧线 + 指针），扩展图标与弹窗头图同步重绘。

### Changed
- **面板 UI 重构**：额度卡片分段展示（用量一行、重置时间独立一行）；每条窗口独立卡片化；双供应商时弹窗自动加宽（360→560px）；百分比数字与进度条状态色联动；数字等宽渲染；更新时间与自动刷新间隔移至底部状态栏。
- 统一图标循环切换间隔口径：后台钳制范围改为 5–60 秒，与设置下拉框、README 一致。

### Fixed
- 文档纠偏：INSTALL.md 移除过时本机路径与 24h 用量描述、修正「GMM」拼写并补充 OpenCode Go 配置步骤；manifest.json 描述移除已下线的 24h 用量字样；README 配置章节与结构树对齐多供应商现状。

### Removed
- 清理死代码：移除 v1.1.0 起不再调用的 24h 模型/工具用量封装（fetchModelUsage / fetchToolUsage / fetchAll / isoRange）与对应端点常量；冒烟测试脚本同步改为仅校验额度接口。

## [1.1.0] - 2026-09-03

### Added
- **多供应商架构**：设置改为复选供应商（GLM 智谱大陆 / OpenCode Go），各自独立配置 API Key。
- **OpenCode Go 接入**：通过官方 `opencode.ai/zen/go/v1/usage` 接口（Bearer API Key）实时显示 rolling 5小时 / 每周 / 每月 用量与重置时间。
- **分栏显示**：两个供应商同时启用时，面板分左右两栏展示各自用量。
- **循环切换徽章**：两个供应商同时启用时，工具栏图标（单行大字号）每隔约 30 秒在两家 5 小时百分比之间循环切换显示；只启用一家时固定显示该家百分比。

### Changed
- 存储结构迁移：由单一 `apiKey` 升级为 `providers.{glm,go}` 结构（自动迁移旧数据）。
- 供应商各自独立拉取，故障隔离：一方失败不影响另一方。
- 移除 GLM 面板「24 小时用量」区块（调用次数 / Tokens / 模型占比 / 联网搜索引擎工具用量）；GLM 不再拉取 24h 用量接口。
- **图标循环切换间隔可配置**：设置中可选 5–60 秒。

## [1.0.4] - 2026-09-03

### Added
- 套餐**到期日期**显示：设置中可手动填写到期日，显示在套餐等级（Lite/Pro/Max）之后；临近到期（≤7 天）变黄、已过期变红。

## [1.0.3] - 2026-09-03

### Changed
- **UI 重构**：5 小时 / 每周额度由环形图改为**横向线性进度条**（对齐 OpenCode Go 风格，深色扁平布局、全中文）。

## [1.0.2] - 2026-09-03

### Fixed
- 修复套餐额度面板不显示的**数据结构不一致问题**：后台写缓存时是平铺结构、返回弹窗时却是嵌套结构，导致弹窗读取的 `limits` 为空；现统一为同一份平铺快照对象。

## [1.0.1] - 2026-09-03

### Changed
- 弹窗**每次打开强制刷新**，规避后台缓存/瞬时错误导致额度卡片不可见。
- 额度查询失败时**显式展示错误原因**（不再静默）。
- UI 配色优化。

## [1.0.0] - 2026-09-03

### Added
- 初始版本：5 小时 / 每周额度、月末 MCP、24h 模型与工具用量、工具栏徽章、自动刷新、设置项（API Key / 刷新间隔）。